@model IEnumerable<GestaoDespesas.Models.ReceitaRecorrente>

@{
    ViewData["Title"] = "Receitas Recorrentes";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <div class="text-muted">Gere receitas que se repetem automaticamente (salários, rendas, etc.).</div>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        Nova recorrente
    </a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">
        Ainda não tens receitas recorrentes definidas.
    </div>
}
else
{
    <table class="table align-middle">
        <thead>
            <tr>
                <th>Descrição</th>
                <th>Tipo</th>
                <th>Frequência</th>
                <th>Início</th>
                <th class="text-end">Valor</th>
                <th class="text-center">Estado</th>
                <th class="text-end" style="width:220px;">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="@(!item.Ativa ? "opacity-50" : "")">
                    <td class="fw-semibold">@item.Descricao</td>
                    <td>@item.Tipo</td>
                    <td>@item.Frequencia</td>
                    <td>@item.DataInicio.ToString("dd/MM/yyyy")</td>
                    <td class="text-end fw-semibold text-success">@item.Valor.ToString("C")</td>
                    <td class="text-center">
                        @if (item.Ativa)
                        {
                            <span class="badge bg-success">Ativa</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">Inativa</span>
                        }
                    </td>
                    <td class="text-end">
                        <a asp-action="Edit" asp-route-id="@item.ReceitaRecorrenteId"
                           class="btn btn-sm btn-outline-secondary">Editar</a>
                        <a asp-action="Delete" asp-route-id="@item.ReceitaRecorrenteId"
                           class="btn btn-sm btn-outline-danger">Apagar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
